# Xaloqi EDS — README (moved)

This file used to be a standalone copy of the project README and had drifted
out of sync (last touched at v1.8.2, missing multiple sections added since).

**The current README is the repo root:** [`../README.md`](../README.md)

This stub exists only so old links into `docs/README.md` don't 404. Do not
duplicate README content here again — see `CONTRIBUTING.md`'s release
checklist, which now points at the root file only.
